infra/
├── docker-compose.yml
├── backend.env   Important : Ne jamais mettre ce fichier sur GitHub !
├── .env.example
├── deploy/
│   └── deploy.sh
├── nginx/
│   └── conf.d/
│       └── default.conf
├── static/
├── media/
├── README.md
└── .gitignore


Voici l’état actuel de ton repository infra (le 3ème repo)
/opt/gasy-car/infra/
├── docker-compose.yml
├── .env
├── deploy/
│   └── deploy.sh
├── nginx/
│   └── conf.d/
│       └── default.conf
├── static/
├── media/
├── .env.example          # (facultatif, tu peux le garder)
├── README.md             # (facultatif)
└── .gitignore


cd /opt/gasy-car/infra

export DOCKERHUB_USERNAME=lalaina14
export IMAGE_TAG=latest

docker compose pull
